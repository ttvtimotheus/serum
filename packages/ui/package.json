{
  "name": "@serum/ui",
  "version": "0.0.1",
  "license": "MIT",
  "type": "module",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "sideEffects": [
    "dist/styles.css"
  ],
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsup src/index.ts --dts --format=esm,cjs --clean && tailwindcss -c tailwind.config.js -i src/styles.css -o dist/styles.css",
    "dev": "tsup src/index.ts --dts --format=esm,cjs --watch",
    "lint": "echo \"add eslint if you want\""
  },
  "peerDependencies": {
    "react": ">=18",
    "react-dom": ">=18"
  },
  "dependencies": {
    "clsx": "^2.1.1"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.10",
    "postcss": "^8.4.47",
    "autoprefixer": "^10.4.20",
    "tsup": "^8.1.0",
    "typescript": "^5.6.3",
    "@serum/tailwind-preset": "workspace:*"
  }
}
